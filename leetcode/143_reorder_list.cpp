struct ListNode {
	int val;
	ListNode* next;

	ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}
};

class Solution {
public:
	void reorderList(ListNode* head) {
		// TODO: implement
	}
};
